<UserControl x:Class="LsrpStreetNamesHud.UI.PathBasedIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="PathBasedIconControl">
    <Canvas Width="{Binding ElementName=PathBasedIconControl, Path=Width}"
            Height="{Binding ElementName=PathBasedIconControl, Path=Height}">

        <Path Fill="{Binding ElementName=PathBasedIconControl, Path=Foreground}"
              Width="{Binding ElementName=PathBasedIconControl, Path=Width}"
              Height="{Binding ElementName=PathBasedIconControl, Path=Height}"
              Stretch="Uniform"
              Data="{Binding ElementName=PathBasedIconControl, Path=IconData}" />

    </Canvas>
</UserControl>
